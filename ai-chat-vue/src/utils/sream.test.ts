/**
 * utils/sream.test.ts —— SSE 流式工具单元测试（待实现）
 *
 * 计划测试的场景：
 *   - 正常 SSE 数据流解析
 *   - 跨 chunk 行截断（buffer 拼接）
 *   - [DONE] 标记结束
 *   - JSON 解析异常行（容错忽略）
 *   - 空流 / 网络中断
 *   - AbortController 中断
 *
 * 当前状态：占位空文件。
 */
